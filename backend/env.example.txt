# Rename to .env and fill in values
MONGODB_URI=mongodb+srv://USER:PASSWORD@cluster.mongodb.net/skillbridge
# Optional local fallback used in development if Atlas SRV DNS lookup fails
MONGODB_LOCAL_URI=mongodb://127.0.0.1:27017/nextcareer
JWT_SECRET=your-super-secret-jwt-key-change-in-production
PORT=5000
NODE_ENV=development

# Comma-separated frontend origins for CORS (production + local)
# Example:
# CORS_ORIGINS=https://your-frontend.onrender.com,https://*.yourdomain.com,http://localhost:5173
CORS_ORIGINS=

# Optional aliases accepted by the backend CORS parser
# FRONTEND_URL=https://your-frontend.onrender.com
# FRONTEND_URLS=https://app.example.com,https://*.example.com
